# Copy to secrets.toml for local Streamlit only — never commit secrets.toml.
# Streamlit Cloud: Settings → Secrets (same keys).

GROQ_API_KEY = "your-groq-api-key"
# GROQ_MODEL = "llama-3.1-8b-instant"
