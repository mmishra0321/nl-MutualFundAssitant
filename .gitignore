# Python virtualenvs
phases/corpus/.venv/
phases/index/.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Local caches (HF models download on deploy / first run)
phases/index/.cache/

# Secrets (Groq API key, etc.)
.env
.streamlit/secrets.toml

# Build artifacts (regenerated by quality pipeline)
phases/quality/hardening_build.json
